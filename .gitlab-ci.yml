image: hugomods/hugo:exts

variables:
  HUGO_ENV: production
  THEME_URL: "github.com/gdr-sdl/theme"

default:
  before_script:
    - hugo version
    - hugo mod get -u $THEME_URL

# Use everywhere except on the default branch
test:
  script:
    - hugo --minify
  rules:
    - if: $CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH

# Run only on the default branch
create-pages:
  stage: deploy
  script:
    - hugo --minify
  pages: true
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
